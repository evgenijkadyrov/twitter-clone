export enum ErrorsResponseCode {
	EMAIL_ALREADY_IN_USE = 'auth/email-already-in-use',
	INVALID_CREDENTIALS = 'auth/invalid-credential',
	UNKNOWN_ERROR = 'auth/user-not-found',
	ERROR_DELETE_TWEET = 'auth/invalid-auth',
}
